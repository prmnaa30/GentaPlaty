// glightbox
const lightbox = GLightbox({
    touchNavigation: true,
    loop: true,
    autoplayVideos: true
});

var lightboxDescription = GLightbox({
    selector: '.glightbox1'
});

// isotope js sorting
